[project]
name = "git-chronos"
version = "0.1.0"
description = "A visual ASCII timeline generator for local Git repositories."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
git-chronos = "git_chronos.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"